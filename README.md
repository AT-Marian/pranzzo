# Pranzzo
Static HTML/CSS/JavaScript marketing website.

Run locally from this folder:
python -m http.server 5500

Then open http://localhost:5500/

The demo form uses mailto and opens the visitor's configured email client with the request addressed to atmarian200114@gmail.com.
